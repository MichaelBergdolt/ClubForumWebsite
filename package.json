{
  "name": "my-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"php -S 0.0.0.0:8000 -t backend\"",
    "build": "npm run build --prefix frontend",
    "build:dev": "npm run build:dev --prefix frontend"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.0",
    "concurrently": "^9.2.1"
  }
}
